#include<iostream>

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(NULL);
    int m,n;
    int s[1001];
    cin >> m >> n;
    for(int i=1;i<=m;i++)
    {
        cin >> s[i];
    }
}